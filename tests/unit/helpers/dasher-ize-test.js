import {
  dasherIze
} from '../../../helpers/dasher-ize';
import { module, test } from 'qunit';

module('DasherIzeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dasherIze(42);
  assert.ok(result);
});
